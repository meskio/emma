There is three layers:
- Core, where the main stuff happends (database, mainloop, ...)
- Interfaces, the ones that comunicates with the world (email, irc, wiki, lorea, ...)
- Modules, the actions and so for the assembly. That should be easy to hack and welcome for every developer.
